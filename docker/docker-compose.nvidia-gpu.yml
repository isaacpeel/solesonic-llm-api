services:
  solesonic-llm-api:
    build:
      context: ..
      dockerfile: docker/Dockerfile.nvidia
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: ["gpu"]
